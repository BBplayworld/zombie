# 게임 로직 구조 다이어그램

## 📊 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      Game Application                        │
│                     (app/page.tsx)                           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   GameEngine (core/)                         │
│  - 게임 루프 관리                                              │
│  - 시스템 통합                                                 │
│  - 엔티티 관리                                                 │
└──┬──────────┬──────────┬──────────┬──────────┬──────────────┘
   │          │          │          │          │
   ▼          ▼          ▼          ▼          ▼
┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────────┐
│Camera│  │TileMap│ │Input │  │Resource│ │Animation │
│      │  │       │ │Manager│ │Loader  │ │          │
└──────┘  └──────┘  └──────┘  └──────┘  └──────────┘
  systems/  systems/  systems/  systems/  systems/

                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
    ┌──────────┐                  ┌──────────┐
    │  Player  │                  │ Monster  │
    │          │                  │          │
    └──────────┘                  └──────────┘
     entities/                     entities/

          │                             │
          └──────────────┬──────────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │   Common Utils      │
              │  - movement.ts      │
              │  - spawn.ts         │
              │  - animation.ts     │
              │  - math.ts          │
              └─────────────────────┘
                      utils/

                         │
                         ▼
              ┌─────────────────────┐
              │   Configuration     │
              │  - types.ts         │
              │  - constants.ts     │
              │  - chapters.ts      │
              └─────────────────────┘
                     config/
```

## 🔄 데이터 흐름

```
1. 게임 시작
   ├─ GameEngine 초기화
   ├─ config/chapters.ts에서 챕터 설정 로드
   ├─ ResourceLoader로 에셋 로드
   └─ 시스템 및 엔티티 초기화

2. 게임 루프 (매 프레임)
   ├─ InputManager에서 입력 받기
   ├─ Player.update()
   │  └─ utils/movement.ts의 공통 함수 사용
   ├─ Monster.update() (각 몬스터)
   │  └─ utils/movement.ts의 공통 함수 사용
   ├─ Camera.update()
   ├─ TileMap.update()
   └─ 렌더링
      ├─ TileMap.render()
      ├─ Entity.render() (Z-sorting)
      └─ UI.render()

3. 몬스터 스폰/리젠
   ├─ config/chapters.ts에서 스폰 설정 확인
   ├─ utils/spawn.ts의 findSafeSpawnPosition() 사용
   └─ Monster 인스턴스 생성
```

## 📦 모듈 의존성

```
┌─────────────────────────────────────────────────────────┐
│                      config/                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ types.ts │  │constants │  │chapters  │              │
│  │          │  │   .ts    │  │   .ts    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│       ▲              ▲              ▲                    │
└───────┼──────────────┼──────────────┼────────────────────┘
        │              │              │
        │              │              │
┌───────┼──────────────┼──────────────┼────────────────────┐
│       │              │              │        utils/       │
│  ┌────┴────┐  ┌──────┴────┐  ┌─────┴─────┐             │
│  │ math.ts │  │movement.ts│  │ spawn.ts  │             │
│  └─────────┘  └───────────┘  └───────────┘             │
│  ┌──────────────────────────────────────┐               │
│  │         animation.ts                 │               │
│  └──────────────────────────────────────┘               │
│       ▲              ▲              ▲                    │
└───────┼──────────────┼──────────────┼────────────────────┘
        │              │              │
        │              │              │
┌───────┼──────────────┼──────────────┼────────────────────┐
│       │              │              │     systems/        │
│  ┌────┴────┐  ┌──────┴────┐  ┌─────┴─────┐             │
│  │ Camera  │  │  TileMap  │  │   Input   │             │
│  │         │  │           │  │  Manager  │             │
│  └─────────┘  └───────────┘  └───────────┘             │
│  ┌──────────────────────────────────────┐               │
│  │      ResourceLoader, Animation       │               │
│  └──────────────────────────────────────┘               │
│       ▲              ▲                                   │
└───────┼──────────────┼───────────────────────────────────┘
        │              │
        │              │
┌───────┼──────────────┼───────────────────────────────────┐
│       │              │           entities/                │
│  ┌────┴────┐  ┌──────┴────┐                              │
│  │ Player  │  │  Monster  │                              │
│  │         │  │           │                              │
│  └─────────┘  └───────────┘                              │
│       ▲              ▲                                   │
└───────┼──────────────┼───────────────────────────────────┘
        │              │
        └──────┬───────┘
               │
┌──────────────┴───────────────────────────────────────────┐
│                    core/GameEngine                        │
│  - 모든 시스템과 엔티티 통합                                 │
│  - 게임 루프 실행                                           │
└──────────────────────────────────────────────────────────┘
```

## 🎯 주요 개선 포인트

### Before (기존 구조)

```
lib/game/
├── useGameEngine.ts      (모든 로직 포함, 700+ 줄)
├── usePlayer.ts          (중복 로직 많음)
├── useMonster.ts         (중복 로직 많음)
├── useTileMap.ts
├── useCamera.ts
├── useInputManager.ts
├── useResourceLoader.ts
├── useSpriteAnimation.ts
├── useGameMath.ts
└── useChapterConfig.ts   (설정 + 로직 혼재)
```

**문제점:**

- ❌ 설정과 로직이 혼재
- ❌ Player와 Monster에 중복 코드
- ❌ 매직 넘버가 코드 곳곳에 존재
- ❌ 파일 이름이 일관성 없음 (use\* 접두사)
- ❌ 재사용 가능한 함수가 분리되지 않음

### After (새로운 구조)

```
lib/game/
├── config/              ✅ 설정만 집중
│   ├── types.ts
│   ├── constants.ts
│   └── chapters.ts
├── utils/               ✅ 재사용 가능한 함수
│   ├── math.ts
│   ├── movement.ts
│   ├── spawn.ts
│   └── animation.ts
├── systems/             ✅ 독립적인 시스템
│   ├── Camera.ts
│   ├── TileMap.ts
│   ├── InputManager.ts
│   ├── ResourceLoader.ts
│   └── SpriteAnimation.ts
├── entities/            ✅ 게임 오브젝트
│   ├── Player.ts
│   └── Monster.ts
└── core/                ✅ 게임 엔진 코어
    └── GameEngine.ts
```

**개선점:**

- ✅ 명확한 책임 분리
- ✅ 공통 로직 통일
- ✅ 의미 있는 상수 사용
- ✅ 일관된 네이밍
- ✅ 높은 재사용성

## 🔧 함수 호출 흐름 예시

### 플레이어 이동 처리

```
User Input (방향키)
    │
    ▼
InputManager.getMovementInput()
    │
    ▼
Player.move(moveX, moveY)
    │
    ├─ config/chapters.ts에서 설정 로드
    │  └─ baseSpeed, collisionYOffset 등
    │
    ├─ utils/movement.ts 함수 사용
    │  ├─ normalizeVector()
    │  ├─ isPositionWalkable()
    │  └─ clampToBoundary()
    │
    └─ Player.velocity 업데이트
        │
        ▼
Player.update(deltaTime)
    │
    ├─ utils/movement.ts
    │  └─ processEntityMovement()
    │      ├─ X축 이동 시도
    │      ├─ Y축 이동 시도
    │      └─ 경계 체크
    │
    └─ utils/animation.ts
        └─ playStateAnimation()
```

### 몬스터 스폰

```
GameEngine.spawnMonsters()
    │
    ├─ config/chapters.ts
    │  └─ 스폰 개수, 몬스터 종류 확인
    │
    ├─ utils/spawn.ts
    │  ├─ findSafeSpawnPosition()
    │  │  ├─ 랜덤 위치 생성
    │  │  ├─ isPositionWalkable() 확인
    │  │  └─ 플레이어와 거리 확인
    │  │
    │  ├─ selectRandomMonsterConfig()
    │  └─ generateMonsterId()
    │
    └─ new Monster(id, x, y, config)
        │
        ├─ utils/animation.ts
        │  └─ setupBasicAnimations()
        │
        └─ Monster 인스턴스 생성 완료
```

## 📝 코드 비교 예시

### 이동 처리

**Before:**

```typescript
// Player.ts에 직접 구현 (100+ 줄)
const nextX = this.position.x + moveX
if (this.tileMap?.isWalkableAtWorld(nextX, this.position.y + 80, 0)) {
  this.position.x = nextX
}
// Monster.ts에도 동일한 코드 중복...
```

**After:**

```typescript
// 공통 함수 사용 (1줄)
processEntityMovement(this.position, this.velocity, deltaTime, this.tileMap)
```

### 설정 접근

**Before:**

```typescript
// 매직 넘버 사용
if (distToPlayer < 500) continue
const SPAWN_MARGIN = 100
```

**After:**

```typescript
// 의미 있는 상수 사용
import { MOVEMENT } from "./config"
if (distToPlayer < MOVEMENT.PLAYER_SAFE_DISTANCE) continue
const margin = MOVEMENT.SPAWN_MARGIN
```

## 🎓 핵심 원칙 요약

1. **DRY (Don't Repeat Yourself)**
   - 중복 코드를 utils/로 추출

2. **Single Responsibility**
   - 각 파일/함수는 하나의 책임만

3. **Clear Naming**
   - 명확한 이름으로 가독성 향상

4. **Centralized Config**
   - 모든 설정은 config/에서 관리

5. **Type Safety**
   - TypeScript 타입 시스템 최대 활용
